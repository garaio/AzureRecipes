namespace LockHandler.Constants
{
    public static class ConfigurationValues
    {
        public const string ServiceBusConnectionString = nameof(ServiceBusConnectionString);

        public const string RenewalIntervalInSeconds = nameof(RenewalIntervalInSeconds);

        public const string MaxRetryNumber = nameof(MaxRetryNumber);
    }
}